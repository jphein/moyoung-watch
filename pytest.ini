[pytest]
# Integration-level offline tests (proto + camera + HA package structure). They insert the
# vendored proto path themselves, so no PYTHONPATH is needed and Home Assistant is NOT required.
# The standalone client has its own suite: `cd client && pytest`.
testpaths = tests
addopts = -q
